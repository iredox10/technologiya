---
import MainLayout from '../../layouts/MainLayout.astro';
import ArticleCard from '../../components/ArticleCard';
import HausaTTS from '../../components/HausaTTS';
import CommentSection from '../../components/CommentSection';
import { articleService, categoryService, authorService } from '../../lib/appwriteServices';
import type { Article, Category, Author } from '../../types';
import { formatHausaDate, getReadingTime } from '../../utils/hausa';

export async function getStaticPaths() {
  const articlesResult = await articleService.getArticles();
  
  if (!articlesResult.success || !articlesResult.data) {
    return [];
  }

  const articles = articlesResult.data.documents as unknown as Article[];
  
  return articles.map((article) => ({
    params: { slug: article.slug },
    props: { article },
  }));
}

const { article: propsArticle } = Astro.props;
const { slug } = Astro.params;

// Use article from props if available (static build), otherwise fetch by slug (dev mode)
let article: Article;
let category: Category;
let author: Author;

if (propsArticle) {
  // Static build - use props
  article = propsArticle;
} else {
  // Dev mode or client-side navigation - fetch by slug
  const articlesResult = await articleService.getArticles();
  if (!articlesResult.success || !articlesResult.data) {
    return Astro.redirect('/404');
  }
  
  const articles = articlesResult.data.documents as unknown as Article[];
  const foundArticle = articles.find(a => a.slug === slug);
  
  if (!foundArticle) {
    return Astro.redirect('/404');
  }
  
  article = foundArticle;
}

// Fetch category data
const categoryResult = await categoryService.getCategory(article.categoryId);
category = categoryResult.success && categoryResult.data 
  ? (categoryResult.data as unknown as Category)
  : { name: 'Uncategorized', slug: 'uncategorized' } as any;

// Fetch author data
const authorResult = await authorService.getAuthor(article.authorId);
author = authorResult.success && authorResult.data
  ? (authorResult.data as unknown as Author)
  : { name: 'Unknown Author', bio: '', avatar: '' } as any;

// Fetch related articles (same category)
const relatedArticlesResult = await articleService.getArticles();
let enrichedRelatedArticles: any[] = [];

if (relatedArticlesResult.success && relatedArticlesResult.data) {
  const rawRelatedArticles = (relatedArticlesResult.data.documents as unknown as Article[])
    .filter(a => 
      a.categoryId === article.categoryId && 
      a.$id !== article.$id &&
      a.status === 'published'
    )
    .slice(0, 3);

  // Enrich articles with category and author data
  enrichedRelatedArticles = await Promise.all(
    rawRelatedArticles.map(async (relArticle) => {
      const relCategoryResult = await categoryService.getCategory(relArticle.categoryId);
      const relAuthorResult = await authorService.getAuthor(relArticle.authorId);
      
      return {
        ...relArticle,
        category: relCategoryResult.success && relCategoryResult.data 
          ? relCategoryResult.data 
          : { name: 'Uncategorized', slug: 'uncategorized' },
        author: relAuthorResult.success && relAuthorResult.data
          ? relAuthorResult.data
          : { name: 'Unknown', avatar: '' },
      };
    })
  );
}

const shareUrl = `${Astro.site || 'https://technologiya.com'}/articles/${article.slug}`;
const shareTitle = encodeURIComponent(article.title);
---

<MainLayout
  title={`${article.title} - Technologiya`}
  description={article.excerpt}
  image={article.coverImage}
>
  <article class="container mx-auto px-4 py-8 max-w-4xl">
    
    <!-- Article Header -->
    <header class="mb-8">
      <!-- Category Badge -->
      <div class="mb-4">
        <a 
          href={`/category/${category.slug}`}
          class="category-badge inline-block px-4 py-1.5 text-sm font-semibold text-white bg-blue-600 hover:bg-blue-700 rounded-full transition-colors tracking-wider uppercase"
        >
          {category.name}
        </a>
      </div>

      <!-- Title -->
      <h1 class="font-display text-5xl md:text-6xl lg:text-7xl font-bold text-gray-900 dark:text-gray-100 mb-6 leading-tight tracking-tight">
        {article.title}
      </h1>

      <!-- Meta Information -->
      <div class="flex flex-wrap items-center gap-6 text-gray-600 dark:text-gray-400 mb-6">
        <div class="flex items-center space-x-2">
          {author.avatar && (
            <img 
              src={author.avatar} 
              alt={author.name}
              class="w-10 h-10 rounded-full"
            />
          )}
          <div>
            <p class="font-mono text-sm font-medium text-gray-900 dark:text-gray-100">
              {author.name}
            </p>
            <p class="text-xs text-gray-500 dark:text-gray-500">
              Marubucin labari
            </p>
          </div>
        </div>

        <div class="meta-info flex items-center space-x-1 text-sm tabular-nums">
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd"></path>
          </svg>
          <span>{formatHausaDate(article.publishedAt || article.$createdAt, 'full')}</span>
        </div>

        <div class="meta-info flex items-center space-x-1 text-sm tabular-nums">
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"></path>
          </svg>
          <span>{getReadingTime(article.content)}</span>
        </div>

        {article.views && (
          <div class="stat-number flex items-center space-x-1 text-sm tabular-nums">
            <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
              <path d="M10 12a2 2 0 100-4 2 2 0 000 4z"></path>
              <path fill-rule="evenodd" d="M.458 10C1.732 5.943 5.522 3 10 3s8.268 2.943 9.542 7c-1.274 4.057-5.064 7-9.542 7S1.732 14.057.458 10zM14 10a4 4 0 11-8 0 4 4 0 018 0z" clip-rule="evenodd"></path>
            </svg>
            <span>{article.views.toLocaleString()} kallonin</span>
          </div>
        )}
      </div>

      <!-- Tags -->
      {article.tags.length > 0 && (
        <div class="flex flex-wrap gap-2 mb-6">
          {article.tags.map((tag) => (
            <span class="font-mono px-3 py-1 text-xs font-medium bg-gray-100 dark:bg-gray-800 text-gray-700 dark:text-gray-300 rounded-full tracking-wide">
              #{tag}
            </span>
          ))}
        </div>
      )}

      <!-- Featured Image -->
      <div class="relative rounded-xl overflow-hidden shadow-2xl mb-8">
        <img 
          src={article.coverImage} 
          alt={article.title}
          class="w-full h-auto"
        />
      </div>

      <!-- Share Buttons -->
      <div class="flex items-center space-x-3 mb-8 p-4 bg-gray-50 dark:bg-gray-800 rounded-lg">
        <svg class="w-5 h-5 text-gray-600 dark:text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z"></path>
        </svg>
        <span class="text-sm font-medium text-gray-700 dark:text-gray-300 mr-2">Raba:</span>
        
        <a
          href={`https://www.facebook.com/sharer/sharer.php?u=${shareUrl}`}
          target="_blank"
          rel="noopener noreferrer"
          class="p-2 rounded-lg bg-blue-600 hover:bg-blue-700 text-white transition-colors"
          aria-label="Raba a Facebook"
        >
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
          </svg>
        </a>

        <a
          href={`https://twitter.com/intent/tweet?text=${shareTitle}&url=${shareUrl}`}
          target="_blank"
          rel="noopener noreferrer"
          class="p-2 rounded-lg bg-sky-500 hover:bg-sky-600 text-white transition-colors"
          aria-label="Raba a Twitter"
        >
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
            <path d="M23.953 4.57a10 10 0 01-2.825.775 4.958 4.958 0 002.163-2.723c-.951.555-2.005.959-3.127 1.184a4.92 4.92 0 00-8.384 4.482C7.69 8.095 4.067 6.13 1.64 3.162a4.822 4.822 0 00-.666 2.475c0 1.71.87 3.213 2.188 4.096a4.904 4.904 0 01-2.228-.616v.06a4.923 4.923 0 003.946 4.827 4.996 4.996 0 01-2.212.085 4.936 4.936 0 004.604 3.417 9.867 9.867 0 01-6.102 2.105c-.39 0-.779-.023-1.17-.067a13.995 13.995 0 007.557 2.209c9.053 0 13.998-7.496 13.998-13.985 0-.21 0-.42-.015-.63A9.935 9.935 0 0024 4.59z"/>
          </svg>
        </a>

        <a
          href={`https://wa.me/?text=${shareTitle}%20${shareUrl}`}
          target="_blank"
          rel="noopener noreferrer"
          class="p-2 rounded-lg bg-green-600 hover:bg-green-700 text-white transition-colors"
          aria-label="Raba a WhatsApp"
        >
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
            <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z"/>
          </svg>
        </a>

        <a
          href={`https://www.linkedin.com/sharing/share-offsite/?url=${shareUrl}`}
          target="_blank"
          rel="noopener noreferrer"
          class="p-2 rounded-lg bg-blue-700 hover:bg-blue-800 text-white transition-colors"
          aria-label="Raba a LinkedIn"
        >
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
            <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
          </svg>
        </a>
      </div>
    </header>

    <!-- Text-to-Speech Component -->
    <HausaTTS 
      text={article.content} 
      articleId={article.$id}
      apiKey={import.meta.env.PUBLIC_HUGGINGFACE_API_KEY}
      client:load 
    />

    <!-- Article Content with Enhanced Typography -->
    <div class="prose prose-lg lg:prose-xl dark:prose-invert max-w-none mb-12
                /* Headings - Bold and Prominent */
                prose-headings:font-display prose-headings:font-black prose-headings:tracking-tight
                prose-headings:text-gray-900 dark:prose-headings:text-gray-100
                prose-headings:scroll-mt-24
                
                /* H1 - Main Title */
                prose-h1:text-4xl sm:prose-h1:text-5xl prose-h1:mb-10 prose-h1:mt-16
                
                /* H2 - Major Sections */
                prose-h2:text-3xl sm:prose-h2:text-4xl prose-h2:mb-8 prose-h2:mt-16
                prose-h2:pb-4 prose-h2:border-b-2 prose-h2:border-blue-200 dark:prose-h2:border-blue-800
                
                /* H3 - Subsections */
                prose-h3:text-2xl sm:prose-h3:text-3xl prose-h3:mb-6 prose-h3:mt-12
                
                /* H4 - Minor Sections */
                prose-h4:text-xl sm:prose-h4:text-2xl prose-h4:mb-5 prose-h4:mt-10
                
                /* Paragraphs - Optimized for Reading */
                prose-p:text-gray-800 dark:prose-p:text-gray-200
                prose-p:text-lg sm:prose-p:text-xl lg:prose-p:text-2xl
                prose-p:leading-[1.8] sm:prose-p:leading-[1.9]
                prose-p:m-8
                
                /* First Paragraph - Standout */
                first:prose-p:text-xl first:prose-p:sm:text-2xl first:prose-p:lg:text-3xl
                first:prose-p:font-light first:prose-p:text-gray-700 dark:first:prose-p:text-gray-300
                
                /* Links */
                prose-a:text-blue-600 dark:prose-a:text-blue-400 
                prose-a:no-underline prose-a:font-semibold
                hover:prose-a:underline prose-a:underline-offset-4
                
                /* Strong & Emphasis */
                prose-strong:text-gray-900 dark:prose-strong:text-gray-100 prose-strong:font-bold
                prose-em:text-gray-800 dark:prose-em:text-gray-200 prose-em:italic
                
                /* Lists - Spacious */
                prose-ul:my-8 prose-ul:space-y-3
                prose-ol:my-8 prose-ol:space-y-3
                prose-li:text-lg sm:prose-li:text-xl lg:prose-li:text-2xl
                prose-li:text-gray-800 dark:prose-li:text-gray-200 
                prose-li:leading-relaxed
                prose-li:marker:text-blue-600 dark:prose-li:marker:text-blue-400
                prose-li:marker:font-bold
                
                /* Blockquotes - Styled */
                prose-blockquote:border-l-4 prose-blockquote:border-blue-600 
                prose-blockquote:bg-blue-50 dark:prose-blockquote:bg-blue-900/10
                prose-blockquote:pl-8 prose-blockquote:pr-6 prose-blockquote:py-6
                prose-blockquote:my-10 prose-blockquote:rounded-r-lg
                prose-blockquote:italic prose-blockquote:text-xl
                prose-blockquote:text-gray-700 dark:prose-blockquote:text-gray-300
                
                /* Code */
                prose-code:text-blue-600 dark:prose-code:text-blue-400 
                prose-code:bg-blue-50 dark:prose-code:bg-blue-900/20
                prose-code:px-2 prose-code:py-1 prose-code:rounded 
                prose-code:text-base prose-code:font-mono
                prose-code:before:content-[''] prose-code:after:content-['']
                
                /* Code Blocks */
                prose-pre:bg-gray-900 dark:prose-pre:bg-gray-950 
                prose-pre:text-gray-100 prose-pre:rounded-xl
                prose-pre:p-6 prose-pre:my-10 prose-pre:shadow-2xl
                prose-pre:overflow-x-auto
                
                /* Images */
                prose-img:rounded-2xl prose-img:shadow-2xl 
                prose-img:my-12 prose-img:w-full
                prose-img:border-4 prose-img:border-white dark:prose-img:border-gray-800
                
                /* HR */
                prose-hr:border-gray-300 dark:prose-hr:border-gray-700 
                prose-hr:my-12 prose-hr:border-t-2
                
                /* Tables */
                prose-table:my-10 prose-table:shadow-lg 
                prose-table:rounded-lg prose-table:overflow-hidden
                prose-thead:bg-gray-100 dark:prose-thead:bg-gray-800
                prose-th:p-4 prose-th:text-left prose-th:font-bold
                prose-td:p-4 prose-td:border-t 
                prose-td:border-gray-200 dark:prose-td:border-gray-700
                ">
      <div set:html={article.content} />
    </div>

    <!-- Author Bio -->
    {author.bio && (
      <div class="bg-gray-50 dark:bg-gray-800 rounded-xl p-6 mb-12">
        <h3 class="text-lg font-bold text-gray-900 dark:text-gray-100 mb-4 flex items-center">
          <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd"></path>
          </svg>
          Game da Marubucin
        </h3>
        <div class="flex items-start space-x-4">
          {author.avatar && (
            <img 
              src={author.avatar} 
              alt={author.name}
              class="w-16 h-16 rounded-full"
            />
          )}
          <div>
            <h4 class="font-bold text-gray-900 dark:text-gray-100 mb-1">
              {author.name}
            </h4>
            <p class="text-gray-600 dark:text-gray-400 text-sm">
              {author.bio}
            </p>
          </div>
        </div>
      </div>
    )}

    <!-- Comment Section -->
    <CommentSection articleId={article.$id} client:load />

    <!-- Related Articles -->
    {enrichedRelatedArticles.length > 0 && (
      <section class="mt-16 pt-8 border-t border-gray-200 dark:border-gray-800">
        <h2 class="text-2xl font-bold text-gray-900 dark:text-gray-100 mb-6">
          Labarai Masu Alaƙa
        </h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          {enrichedRelatedArticles.map((related) => (
            <ArticleCard article={related} client:visible />
          ))}
        </div>
      </section>
    )}

  </article>
</MainLayout>
